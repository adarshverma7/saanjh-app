<?xml version="1.0" encoding="utf-8"?>
<!--
  Saanjh Home Screen Widget — 2×2 layout.
  Displays: contact name, pulse presence, streak count.
  Background: dark amber-tinted (#1A0800) matching app theme.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="12dp"
    android:background="@drawable/saanjh_widget_bg">

    <!-- Contact name -->
    <TextView
        android:id="@+id/widget_contact_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="—"
        android:textColor="#F5EFE8"
        android:textSize="16sp"
        android:fontFamily="sans-serif-medium"
        android:ellipsize="end"
        android:maxLines="1" />

    <!-- Pulse presence -->
    <TextView
        android:id="@+id/widget_pulse_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text=""
        android:textColor="#FF9500"
        android:textSize="11sp" />

    <!-- Streak count -->
    <TextView
        android:id="@+id/widget_streak"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="🔥 —"
        android:textColor="#FFA040"
        android:textSize="13sp"
        android:fontFamily="sans-serif-medium" />

</LinearLayout>
